# 版本更新说明

## v1.0 初始版本  
- 实现了最基础的聊天功能  
- 支持用户通过命令行进行简单的消息收发  
- 采用本地内存存储，无持久化能力  
- 无用户认证机制，安全性较弱  

---

## v2.0 功能增强版  
- **消息漫游**：用户在不同设备或重新登录后，可同步查看历史消息  
- **消息持久化**：引入 SQLite 数据库存储用户信息与聊天记录（`users.db`）  
- **UI 界面美化**：前端改用 PyQt6 构建图形化界面，提升用户体验  
- 增加基本的用户注册与登录流程  
- 支持私聊与群聊两种模式  

---

## v3.0 安全强化与平台整合版  
- **全面安全增强**：
  - 所有通信均通过 WebSocket + FastAPI 进行加密传输（建议配合 HTTPS/WSS 部署）
  - 用户登录需提供用户名与密码双重验证
  - 自动生成 10 位唯一用户 ID，避免用户名冲突，提高系统健壮性
- **全平台消息同步**：
  - 私聊与群聊消息均支持跨设备历史同步
  - 消息按会话分类存储，支持按对话加载历史记录
- **智能消息投递**：
  - 支持 `@用户名 内容` 语法实现定向私聊
  - 系统自动识别并路由私聊消息至目标用户
- **日志与调试优化**：
  - 全局日志系统，关键操作标记为 `[LOG]`
  - 后端使用 Uvicorn + FastAPI 提供 REST API 与 WebSocket 双通道
  - 客户端基于 `aiohttp` + `websockets` + `qasync` 实现异步非阻塞通信
- **用户体验提升**：
  - 登录/注册独立 UI 界面（`login_ui.py` / `register_ui.py`）
  - 聊天主界面支持实时消息刷新、时间戳显示、历史消息分页加载
  - 自动防止同一账号多端重复登录（后登录者踢出前会话）
- **部署与兼容性**：
  - 默认监听 `0.0.0.0:12345`，便于局域网内快速测试
  - Windows 平台适配 asyncio 事件循环策略，确保稳定运行
  - 使用 `python -B` 启动可避免生成 `__pycache__` 文件，保持目录整洁

---
## v3.1 版本更新说明

## 核心升级亮点

### 1. **真实用户名全程同步，彻底解决身份混淆问题**
- 登录后服务器返回 **真实用户名（real_username）**，客户端 UI 标题、消息渲染、私聊逻辑均基于此身份
- 避免因用户使用 10 位 ID 登录导致界面显示混乱或私聊失败的问题
- 聊天窗口标题格式更新为：`老莫聊天室-当前用户{username}（{user_id}）`，清晰展示双重标识

### 2. **全函数操作日志覆盖（仅 print，无性能侵入）**
- 服务端 `server.py` 中 **所有核心函数**（注册、登录、消息存储、连接管理等）均添加 `[LOG]` 日志输出
- 便于开发者快速追踪请求流程、调试连接异常、验证消息投递路径
- 日志内容包含关键参数（如用户名、消息类型、临时ID等），提升可观察性

### 3. **认证流程健壮性增强**
- WebSocket 连接采用 **两阶段认证**：
  1. 先建立 `pending_connections` 临时连接
  2. 收到 `login` 消息后校验凭证，成功则提升为正式用户
- **重复登录自动踢人**：同一账号在新设备登录时，旧会话将被主动关闭（WebSocket code 4000）
- 认证成功后立即向客户端推送 `{"type": "auth_success", "username": "真实用户名"}`，确保前端状态同步

### 4. **UI/UX 细节打磨**
- **注册成功提示**：明确展示分配的 10 位用户 ID，并提示“请妥善保存！”
- **登录历史智能补全**：输入框聚焦时自动弹出历史账号列表（支持模糊匹配）
- **系统通知样式升级**：上线/下线提示采用半透明亚克力风格（`rgba(240,240,240,0.8)`），居中显示更美观
- **消息时间格式优化**：
  - 同日消息显示 `HH:MM:SS`
  - 跨日消息显示 `YYYY年MM月DD日 HH:MM:SS`

### 5. **客户端稳定性提升**
- 使用 `websockets.State.OPEN` 精确判断连接状态，避免向已关闭连接发送数据
- 接收循环 (`receive_loop`) 能正确捕获 `ConnectionClosed` 异常并触发 `on_disconnect` 回调
- 主程序 (`main.py`) 通过 `call_soon_threadsafe` 确保断开回调在主线程安全执行 UI 操作（如弹窗、跳转）

---

## 技术细节改进

| 模块 | 改进点 |
|------|--------|
| **server.py** | - `authenticate()` 返回真实用户名<br>- `send_welcome()` 包含离线消息+历史漫游+上线广播三合一初始化<br>- 所有数据库操作增加异常捕获与日志<br>- 将用户数据与聊天数据分开 |
| **client.py** | - 新增 `real_username` 字段<br>- `send_packet()` 增加连接状态检查与详细调试日志<br>- 支持 `websockets >=15.0` 无 `extra_headers` 连接方式 |
| **chat_ui.py** | - `render_message()` 统一处理所有消息类型的时间与身份显示<br>- 私聊消息区分“我发给他人”和“他人发给我”<br>- 历史消息加载前做类型校验，防止字符串误传 |
| **main.py** | - 登录成功后保存 **真实用户名** 到 QSettings，确保历史记录准确<br>- 断开连接后自动返回登录界面，体验闭环 |

v3.1 聚焦 **身份一致性** 与 **可观测性**，为后续多群组、文件传输等高级功能打下坚实基础。

> **技术栈**：FastAPI + Uvicorn + WebSocket + SQLite + PyQt6 + qasync + aiohttp  
> **数据库**：`users.db`（含 `users` 与 `messages` 表）  
> **WebSocket 地址**：`ws://<服务器IP>:12345/ws`  
> **作者**：MoYifan  
> **发布日期**：2025 年  