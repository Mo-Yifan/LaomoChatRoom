# 版本更新说明

## v1.0 初始版本  
- 实现了最基础的聊天功能  
- 支持用户通过命令行进行简单的消息收发  
- 采用本地内存存储，无持久化能力  
- 无用户认证机制，安全性较弱  

---

## v2.0 功能增强版  
- **消息漫游**：用户在不同设备或重新登录后，可同步查看历史消息  
- **消息持久化**：引入 SQLite 数据库存储用户信息与聊天记录（`users.db`）  
- **UI 界面美化**：前端改用 PyQt6 构建图形化界面，提升用户体验  
- 增加基本的用户注册与登录流程  
- 支持私聊与群聊两种模式  

---

## v3.0 安全强化与平台整合版  
- **全面安全增强**：
  - 所有通信均通过 WebSocket + FastAPI 进行加密传输（建议配合 HTTPS/WSS 部署）
  - 用户登录需提供用户名与密码双重验证
  - 自动生成 10 位唯一用户 ID，避免用户名冲突，提高系统健壮性
- **全平台消息同步**：
  - 私聊与群聊消息均支持跨设备历史同步
  - 消息按会话分类存储，支持按对话加载历史记录
- **智能消息投递**：
  - 支持 `@用户名 内容` 语法实现定向私聊
  - 系统自动识别并路由私聊消息至目标用户
- **日志与调试优化**：
  - 全局日志系统，关键操作标记为 `[LOG]`
  - 后端使用 Uvicorn + FastAPI 提供 REST API 与 WebSocket 双通道
  - 客户端基于 `aiohttp` + `websockets` + `qasync` 实现异步非阻塞通信
- **用户体验提升**：
  - 登录/注册独立 UI 界面（`login_ui.py` / `register_ui.py`）
  - 聊天主界面支持实时消息刷新、时间戳显示、历史消息分页加载
  - 自动防止同一账号多端重复登录（后登录者踢出前会话）
- **部署与兼容性**：
  - 默认监听 `0.0.0.0:12345`，便于局域网内快速测试
  - Windows 平台适配 asyncio 事件循环策略，确保稳定运行
  - 使用 `python -B` 启动可避免生成 `__pycache__` 文件，保持目录整洁

---
## v3.1 版本更新说明

## 核心升级亮点

### 1. **真实用户名全程同步，彻底解决身份混淆问题**
- 登录后服务器返回 **真实用户名（real_username）**，客户端 UI 标题、消息渲染、私聊逻辑均基于此身份
- 避免因用户使用 10 位 ID 登录导致界面显示混乱或私聊失败的问题
- 聊天窗口标题格式更新为：`老莫聊天室-当前用户{username}（{user_id}）`，清晰展示双重标识

### 2. **全函数操作日志覆盖（仅 print，无性能侵入）**
- 服务端 `server.py` 中 **所有核心函数**（注册、登录、消息存储、连接管理等）均添加 `[LOG]` 日志输出
- 便于开发者快速追踪请求流程、调试连接异常、验证消息投递路径
- 日志内容包含关键参数（如用户名、消息类型、临时ID等），提升可观察性

### 3. **认证流程健壮性增强**
- WebSocket 连接采用 **两阶段认证**：
  1. 先建立 `pending_connections` 临时连接
  2. 收到 `login` 消息后校验凭证，成功则提升为正式用户
- **重复登录自动踢人**：同一账号在新设备登录时，旧会话将被主动关闭（WebSocket code 4000）
- 认证成功后立即向客户端推送 `{"type": "auth_success", "username": "真实用户名"}`，确保前端状态同步

### 4. **UI/UX 细节打磨**
- **注册成功提示**：明确展示分配的 10 位用户 ID，并提示“请妥善保存！”
- **登录历史智能补全**：输入框聚焦时自动弹出历史账号列表（支持模糊匹配）
- **系统通知样式升级**：上线/下线提示采用半透明亚克力风格（`rgba(240,240,240,0.8)`），居中显示更美观
- **消息时间格式优化**：
  - 同日消息显示 `HH:MM:SS`
  - 跨日消息显示 `YYYY年MM月DD日 HH:MM:SS`

### 5. **客户端稳定性提升**
- 使用 `websockets.State.OPEN` 精确判断连接状态，避免向已关闭连接发送数据
- 接收循环 (`receive_loop`) 能正确捕获 `ConnectionClosed` 异常并触发 `on_disconnect` 回调
- 主程序 (`main.py`) 通过 `call_soon_threadsafe` 确保断开回调在主线程安全执行 UI 操作（如弹窗、跳转）

---

## 技术细节改进

| 模块 | 改进点 |
|------|--------|
| **server.py** | - `authenticate()` 返回真实用户名<br>- `send_welcome()` 包含离线消息+历史漫游+上线广播三合一初始化<br>- 所有数据库操作增加异常捕获与日志<br>- 将用户数据与聊天数据分开 |
| **client.py** | - 新增 `real_username` 字段<br>- `send_packet()` 增加连接状态检查与详细调试日志<br>- 支持 `websockets >=15.0` 无 `extra_headers` 连接方式 |
| **chat_ui.py** | - `render_message()` 统一处理所有消息类型的时间与身份显示<br>- 私聊消息区分“我发给他人”和“他人发给我”<br>- 历史消息加载前做类型校验，防止字符串误传 |
| **main.py** | - 登录成功后保存 **真实用户名** 到 QSettings，确保历史记录准确<br>- 断开连接后自动返回登录界面，体验闭环 |

v3.1 聚焦 **身份一致性** 与 **可观测性**，为后续多群组、文件传输等高级功能打下坚实基础。

---

# v4.0 群组生态与体验重构版

> **从“能聊天”到“好聊天”的全面跃迁**  
> 本版本聚焦**群组能力闭环**、**身份一致性强化**与**多会话管理体验**，彻底告别“世界喇叭”时代。

---

## 🌐 核心升级亮点

### 1. 全功能群组系统上线（重大新增）
- **动态创建群聊**：用户可随时创建命名群组，系统自动生成唯一群 ID（格式 `G` + 9 位数字，如 `G123456789`）
- **自由加入/退出机制**：通过群 ID 加入他人创建的群；支持主动退出群聊
- **群消息隔离广播**：群内消息仅投递给该群成员，与私聊、其他群完全隔离
- **群列表持久化同步**：登录后自动拉取“我的群组”列表，跨设备一致呈现
- **UI 深度集成**：
  - 左侧会话栏独立展示群聊（👥 图标）与私聊（👤 图标）
  - 聊天标题动态显示当前群名或私聊对象状态（在线/离线）

### 2. 身份系统终极统一（解决历史顽疾）
- **全程使用真实用户名通信**：
  - 所有消息字段 `sender` / `target` 均为**真实用户名**（非10位ID）
  - 私聊逻辑、消息渲染、会话匹配全部基于真实身份
- **双重标识清晰展示**：
  - 主窗口标题：`老莫聊天室 - {真实用户名} ({10位ID})`
  - 私聊对象显示：`张三 (在线)` / `李四 (离线)`
- **彻底杜绝“ID私聊失败”问题**：无论用户用 ID 还是用户名登录，系统内部均映射为真实身份

### 3. 多会话管理体验革命
- **左侧会话面板重构**：
  - 顶部固定“创建群聊”、“加入群聊”快捷入口
  - 动态分区展示：所有私聊联系人（含在线状态） + 所有已加入群组
  - 支持会话名称更新（如群名修改后自动同步）
- **会话级消息缓存**：
  - 每个私聊/群聊独立缓存历史消息（`session_history` 字典）
  - 切换会话时即时加载对应历史，流畅无等待
- **智能消息归属识别**：
  - 自动判断消息所属会话（我发给 A 的私聊 vs B 发给我的私聊）
  - 群消息按 `group_id` 归档，避免串扰

### 4. 客户端健壮性再升级
- **连接状态精准管控**：
  - 使用 `websockets.State.OPEN` 严格校验连接有效性
  - 避免向已关闭连接发送数据导致异常
- **异常断连自动恢复**：
  - `receive_loop` 捕获 `ConnectionClosed` 异常
  - 触发 `on_disconnect` 回调，主线程安全返回登录界面
- **日志可观测性增强**：
  - 关键 UI 操作添加 `[DEBUG]` 日志（如消息渲染、会话切换）
  - 网络层 `send_packet()` 输出详细调试信息

### 5. 用户体验细节打磨
- **输入体验优化**：
  - 回车键直接发送消息（无需点击按钮）
  - 输入框高度自适应，支持长文本
- **消息气泡视觉升级**：
  - 自己发送的消息：浅青色背景（`#f0ffff`）
  - 他人消息：纯白背景
  - 统一圆角、内边距、字体大小（16px）
- **系统通知更友好**：
  - 群创建成功、欢迎回来等提示采用亚克力半透明样式
  - 居中显示，自动融入聊天流

---

## 🛠️ 技术细节改进

| 模块 | 改进点 |
|------|--------|
| **server.py** | - 新增 `/create_group`, `/join_group` REST 接口<br>- WebSocket 消息路由支持 `group_created`/`group_joined` 等新类型<br>- 群消息存储增加 `group_id` 字段，与私聊消息物理隔离 |
| **client.py** | - `ChatClient` 新增群组操作方法（`create_group`, `join_group`）<br>- 消息接收循环兼容新旧格式（自动转换 `from/to/text` → 标准 `message` 结构） |
| **chat_ui.py** | - 重构会话管理逻辑（`add_session_item`, `update_or_add_session`）<br>- 实现 `_cache_and_maybe_display` 统一消息处理入口<br>- 私聊状态实时更新（在线/离线） |
| **main.py** | - 登录成功后拉取“我的群组”列表<br>- 断连后自动跳转登录页，体验闭环 |

---

## 📦 数据库变更

新增 `groups.db` 数据库，包含以下表：
- `groups`：群组元数据（`id`, `name`, `creator`, `created_at`）
- `group_members`：群成员关系（`group_id`, `username`, `joined_at`）

> 原 `users.db` 与 `messages.db` 结构保持兼容，v3.x 用户数据可无缝迁移。

---

## ⚙️ 部署说明

- **服务端**：默认仍监听 `0.0.0.0:12345`，新增群组相关 API 路由
- **客户端**：需升级依赖至 `websockets>=15.0`（移除 `extra_headers` 用法）
- **首次启动**：服务端自动初始化 `groups.db`，无需手动干预

---

> **v4.0 是老莫聊天室的“成人礼”** —— 它不再是一个玩具，而是一个真正可用、好用、值得信赖的私有聊天平台。  
> 下一阶段将探索：文件传输、消息撤回、端到端加密…… 敬请期待！

---

# v5.0 全平台融合与架构统一版

> **一次连接，处处可聊**  
> 本版本实现 **桌面客户端 + Web 浏览器** 双端并行接入同一聊天网络，构建真正意义上的“全平台实时通信生态”，同时完成消息协议标准化与服务架构轻量化。

---

## 🌐 核心升级亮点

### 1. Web 前端正式上线（重大新增）
- **纯 HTML/JS 聊天界面**：新增 `web/chat.html`，无需安装任何软件，浏览器打开即用
- **统一后端服务**：FastAPI 内置静态文件服务，自动挂载 `../web` 目录
- **根路径重定向**：访问 `http://<IP>:12345` 自动跳转至 `/web/chat.html`
- **跨设备无缝体验**：
  - 手机、平板、PC 均可通过浏览器加入聊天
  - 与 PyQt6 桌面客户端共享同一账号体系、消息历史、群组列表

### 2. 消息协议全面标准化（架构级重构）
- **统一消息类型为 `"message"`**：
  - 废弃旧版 `private` / `group` 作为顶层 `type` 的设计
  - 所有实时聊天消息统一使用 `{ "type": "message", "chat_type": "private|group" }`
- **字段语义清晰化**：
  - `from` → 发送者真实用户名（非 ID）
  - `to` → 接收目标（私聊对象用户名 / 群 ID）
  - `text` → 消息内容
  - `timestamp` → ISO8601 格式时间
- **前后端解耦**：Web 与桌面客户端解析逻辑完全一致，降低维护成本

### 3. 服务端架构轻量化与健壮性增强
- **零外部依赖部署**：
  - 无需 Nginx/Apache，FastAPI 自带静态资源服务
  - 单进程 `uvicorn` 同时处理 WebSocket + HTTP + 静态文件
- **路径安全加固**：
  - 静态文件挂载前校验 `../web` 目录是否存在
  - 避免因路径错误导致 404 或启动失败
- **连接管理优化**：
  - 保留 `pending_connections` 两阶段认证机制
  - 重复登录踢人、离线消息推送、上线广播等逻辑完整兼容 Web 端

### 4. 用户体验一致性提升
- **身份标识统一**：
  - Web 端与桌面端均显示 `老莫聊天室 - {真实用户名} ({10位ID})`
  - 私聊对象显示在线状态（需服务端推送 `online_users`）
- **操作流程对齐**：
  - 注册 → 获取 10 位用户 ID（Web 与桌面提示一致）
  - 登录 → 自动拉取“我的群组”、“我的好友”、“离线消息”
  - 发消息 → 回车发送，支持私聊与群聊切换
- **系统通知通用化**：
  - 上线/下线、群创建成功等提示在 Web 端以 `<div>` 形式居中展示
  - 桌面端保持亚克力半透明样式，风格统一

### 5. 开发者体验优化
- **工程结构扁平化**：
  - 所有核心代码位于项目根目录，无多层嵌套
  - `backend/`（原 client+server）→ 实际为完整服务端，命名更准确
- **调试日志全覆盖**：
  - 服务端每个函数入口均有 `[LOG]` 输出（如 `[LOG] create_group(...)`）
  - Web 客户端可通过浏览器控制台查看原始消息帧
- **Windows 兼容性彻底解决**：
  - 移除 `websockets.connect(extra_headers=...)` 用法
  - 支持 `websockets >=15.0` 默认连接方式，稳定运行于所有平台

---

## 🛠️ 技术细节改进

| 模块 | 改进点 |
|------|--------|
| **server.py** | - 新增 `app.mount("/web", StaticFiles(...))`<br>- 根路由 `/` 重定向至 `/web/chat.html`<br>- 所有消息推送统一使用 `{"type": "message", "chat_type": ...}` |
| **chat.html** | - 实现登录/注册表单切换<br>- 动态渲染在线用户列表（点击发起私聊）<br>- 支持群组列表展示与会话切换<br>- 消息区域按时间顺序滚动更新 |
| **数据库** | - 保持 `users.db` / `messages.db` / `groups.db` 三库分离<br>- 无结构变更，v4.x 数据可直接迁移 |
| **部署** | - 启动命令不变：`python -B main.py`（或 `server.py`）<br>- 默认监听 `0.0.0.0:12345`，局域网内任意设备可访问 |

---

## ⚙️ 升级注意事项

- **前端开发者**：Web 端需自行扩展 UI（当前为基础功能），但通信协议已冻结，可安全对接。
- **服务运维**：若需公网访问，请配置反向代理并启用 HTTPS/WSS，避免明文传输。
- **路径依赖**：`server.py` 中静态文件路径为 `../web`，请确保 `web/` 目录与 `server.py` 同级。

---

> **v5.0 标志着老莫聊天室从“桌面工具”迈向“通信平台”**。  
> 未来，我们将在此统一架构上叠加更多能力：消息搜索、文件传输、语音通话……  
> 而这一切，都始于一个简单的 `chat.html`。

**作者**：MoYifan  
**发布日期**：2025 年 12 月